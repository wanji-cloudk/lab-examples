his acts as a clean library index for the general automation scripts you write.

```markdown
# Linux System Automation Utilities

A collection of foundational, standalone shell scripts written to handle environment configurations, system resource checks, and automated maintenance tasks.

## 🛠️ Scripts Included

### 1. `sys_info` / `system_info`
* **Purpose:** Collects operational health metrics from the host operating system.
* **Key Features:** Captures CPU utilization, disk capacity parameters, and active memory distributions, formatting the output for clear logs.

## 🏃 Execution Instructions
Ensure the scripts have executable permissions before running them within your Linux environment:
```bash
chmod +x <script_name>.sh
./<script_name>.sh
